# Example .env.sample for Backend
# Copy this file to .env and fill the real values locally (do NOT commit .env)

# MongoDB connection URI (example)
MONGO_URI=mongodb://username:password@localhost:27017/dbname

# JWT secret used for signing tokens
JWT_SECRET=your_jwt_secret_here

# App port
PORT=5000
